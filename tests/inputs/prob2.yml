translator_input: |-
  num: word 1
  result: word 2

  start:
    mov ecx, 3
    mov eax, 2
  loop:
    mov edx, eax
    add eax, [num]
    mov [num], edx
    dec ecx
    jnz loop
    mov ecx, 3
    cmp eax, 4000000
    jns end
    add [result], eax
    jmp loop

  end:
    mov eax, [result]
    exit



machine_input: foo