use std::path::Path;

use thiserror::Error;

#[derive(Error, Debug)]
pub enum MachineError {
    // todo
}

struct DataPath {
    // todo
}

impl DataPath {
    // todo
}

struct ControlUnit {
    // todo
}

impl ControlUnit {
    // todo
}

pub fn main(code_path: &Path, input_path: &Path) -> Result<(), MachineError> {
    todo!()
}
